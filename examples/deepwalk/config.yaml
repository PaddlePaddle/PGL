model_name: Deepwalk
learning_rate: 0.0025

sample_workers: 10
batch_size: 512
cpu_batch_size: 32 # only used in distributed cpu mode.
neg_num: 20
walk_len: 40
win_size: 10
neg_sample_type: average
embed_size: 128
shared_embedding: False

num_emb_part: 8 # only used in distributed gpu mode.

multiclass_learning_rate: 0.025